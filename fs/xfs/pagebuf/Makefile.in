#
# Makefile for the linux pagebuf routines.
#

expsyms(page_buf.o page_buf_io.o page_buf_locking.o)
objlink(pagebuf.o avl.o page_buf.o page_buf_io.o page_buf_locking.o)
# No select() for xfs/pagebuf in this directory.  It is a sub-component of XFS,
# see fs/xfs/Makefile.in for the objlink.

# Debug options:
#	-DPAGEBUF_TRACKING
#	-DPAGEBUF_LOCKING
#	-DPAGEBUF_DEBUG
#	-DPAGEBUF_TRACE
ifsel(CONFIG_PAGEBUF_DEBUG)
	extra_cflags_all(-g -DSTATIC="" -DPAGEBUF_DEBUG -DPAGEBUF_TRACKING -DPAGEBUF_TRACE)
endif
